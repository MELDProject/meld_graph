name: ci

on:
  push:
    branches:
      - "dev_docker"

env:
  TEST_TAG: user/app:test
  LATEST_TAG: user/app:latest

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
          # this might remove tools that are actually needed,
          # if set to "true" but frees about 6 GB
          tool-cache: false
          
          # all of these default to true, but feel free to set to
          # "false" if necessary for your workflow
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          tags: ${{ env.TEST_TAG }}
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Test
        env: 
          LICENSE_TEXT: ${{ secrets.FS_LICENSE }}
        run: |
          mkdir docker-data
          echo $LICENSE_TEXT >> ./docker-data/license.txt
          docker run -v ./docker-data:/data -v ./docker-data/license.txt:/license.txt -e FS_LICENSE=/license.txt --rm ${{ env.TEST_TAG }} python scripts/new_patient_pipeline/prepare_classifier.py
          docker run -v ./docker-data:/data -v ./docker-data/license.txt:/license.txt -e FS_LICENSE=/license.txt --rm ${{ env.TEST_TAG }} pytest -s


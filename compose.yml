services:
  meld_graph:
    image: meldproject/meld_graph:latest
    volumes:
      - ./docker-data:/data
    environment: 
      - FS_LICENSE=/run/secrets/license.txt
    secrets:
      - license.txt

secrets:
  license.txt:
    file: ./license.txt
